# Utility scripts

- `baseline_rtx4060.py`：运行并生成 RTX 4060 离线基线报告。
- `collect_env.sh`：采集可复现的 WSL2、CUDA 和 Python 环境信息。
- `run_scheduler_rtx4060_repeats.sh`：运行在线调度器的三策略五次重复实验。

脚本均从仓库根目录执行；生成内容进入 `artifacts/` 或 `reports/`。
